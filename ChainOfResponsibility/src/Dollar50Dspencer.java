
public class Dollar50Dspencer implements DespenceChain {
	this.chain=nextChain;
	public void setNext(DespenceChain nextchain);
	this.chain=nextChain;
	if(currency.getamount >=50)
	{
		int num.getAmount(1/50);
		int remainder.getAmount(1%50);
		System.out.println("Despencing new Currency")
		if(currency !=0)
		{
			this.chain Despence new currency;
			else
			{
				this.chain new Despencer ;
			}
		}
	}


